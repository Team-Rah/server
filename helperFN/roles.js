module.exports = {
  //comparing roles
  //two objects: 1: voter {userId: role} 2: candidates {userId: role}
  //check if votes are valid... wolves cant vote for each other...return boolean
  //wolfCheck

  //doctorCheck voter and candidate
  //two arrays: 1: vote if role is doctor 2: player array: if player is dead...change to alive
  //check player array is doctor and alive
  //returning player array witht alive status

};